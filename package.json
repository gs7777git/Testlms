"scripts": {
  "dev": "vite",
  "build": "tsc --declaration && vite build", // Add '--declaration' for declaration files
  "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
  "preview": "vite preview"
}
